#audio
#name: sound volume. format: 0 < int < 300. default: 100 (100%) (wip)
100

#update tick
#used in while true loops to prevent the loop from causing high cpu usage.
#in milliseconds. 1000 milliseconds = 1 second.
#name: menu update tick. format: 0 < int < 2000. default: 100 (10 updates per second) 
100
#name: game update tick. format: 0 < int < 500. default: 50 (20 updates per second) 
50

#name: wip. format: - < T < -. default: null (wip)
wip

#name: map grid. format: true or false. default: false (wip)
false

#overrides a lot of stuff to make the game trash, but good on performance.
#name: performance mode. format: true or false. default: false (wip)
false

#use only if text is turning into "...", this makes the text very small!
#name: tiny gui mode. format: true or false. default: false (wip)
false

#name: cheats. format: true or false. default: false (wip)
false